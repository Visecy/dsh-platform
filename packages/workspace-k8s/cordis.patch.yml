# Native workspace UI client mounted through the official bundle system.
# Row id MUST equal the package name so dsh-client-modules can resolve the
# package's dsh.client declaration and ./client export.
- insert:
    - id: '@visecy/dsh-workspace-k8s'
      name: '@visecy/dsh-workspace-k8s'
